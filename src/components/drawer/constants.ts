export const DEFAULT_SIZE = 280;
export const DEFAULT_MINI_SIZE = 64;
export const DEFAULT_SWIPE_EDGE = 20;
export const DEFAULT_Z_INDEX = 1300;
export const DEFAULT_TRANSITION_DURATION = 220;
export const DEFAULT_TRANSITION_OFFSET = 8;
export const SWIPE_CLOSE_THRESHOLD = 0.35;
export const SWIPE_OPEN_THRESHOLD = 24;

export const FOCUSABLE_SELECTOR = [
  "a[href]",
  "button:not([disabled])",
  "textarea:not([disabled])",
  "input:not([disabled])",
  "select:not([disabled])",
  '[tabindex]:not([tabindex="-1"])',
  '[contenteditable="true"]',
].join(",");
